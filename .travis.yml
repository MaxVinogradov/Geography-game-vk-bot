language: python
python:
    - 3.4
    - 3.5
    
before_install:
    - pip install vk
    - pip install redis
    - pip install coveralls
    
services:
    - redis-server
    
script:
    - coverage run --source=geography-game-vk-bot test.py test
    
after_success:
  coveralls
